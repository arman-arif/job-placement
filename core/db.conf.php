<?php

    $server = "localhost";
    $user = "root";
    $pass = "";
    $database = "job-placement";

    $con = mysqli_connect($server, $user, $pass, $database) or die("Connection failed");
    $db = mysqli_connect($server, $user, $pass, $database);


?>
